"use client";

const CustomButton = () => {
  return (
    <button
      disabled={false}
      type={"button"}
      className="custom-btn"
      onClick={() => {}}
    >
      Explore Cars
    </button>
  );
};

export default CustomButton;
